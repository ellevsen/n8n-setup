# ============================================================
# VPS Setup n8n - Variáveis de ambiente (copie para .env)
# ============================================================
# Se um .env existir nesta pasta, o script carrega antes de pedir
# inputs. Variáveis faltantes serão perguntadas no script.
# Senhas são geradas automaticamente se não definidas.
# ============================================================

# --- Obrigatório: usuário admin Portainer e domínios ---
PORTAINER_ADMIN_USER=admin
DOMAIN_PORTAINER=portainer.seudominio.com
DOMAIN_TRAEFIK_DASHBOARD=traefik.seudominio.com
DOMAIN_N8N_EDITOR=n8n.seudominio.com
DOMAIN_N8N_WEBHOOK=webhook.seudominio.com

# Email para certificados Let's Encrypt (obrigatório para HTTPS)
LETSENCRYPT_EMAIL=seu-email@example.com

# --- Opcional: senhas (deixe vazio para gerar automaticamente) ---
# PORTAINER_ADMIN_PASSWORD=
# POSTGRES_PASSWORD=
# N8N_DB_PASSWORD=
# REDIS_PASSWORD=
# N8N_ENCRYPTION_KEY=

# --- Opcional: pgAdmin (se quiser interface web para o Postgres) ---
# PGADMIN_DOMAIN=pgadmin.seudominio.com
# PGADMIN_USER=admin@local
# PGADMIN_PASSWORD=

# --- Timezone (opcional) ---
TZ=America/Sao_Paulo
